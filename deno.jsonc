{
  "tasks": {
    "dev": "deno run --watch src/main.ts",
    "dev:test": "deno test --watch --allow-read",
    "dev:test:update": "deno test --allow-read --allow-write -- --update",
    "check": "deno check src/main.ts && deno lint && deno fmt --check && deno test --allow-read"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/testing": "jsr:@std/testing@1"
  }
}
